import styled from "styled-components";
import { H1 } from "../../styled.components/shared";

export const Header = styled(H1)`
  text-align: center;
`;

export const ContentContainer = styled.div`
  margin-top: -50px;
`;
